apiVersion: "helm.toolkit.fluxcd.io/v2"
kind: HelmRelease
metadata:
  name: ipsec-policy-agent
  labels:
    chart_group: ipsec-policy-operator-charts
spec:
  releaseName: ipsec-policy-agent
  chart:
    spec:
      chart: ipsec-policy-agent
      version: REPLACE_HELM_CHART_VERSION
      sourceRef:
        kind: HelmRepository
        name: stx-platform
  dependsOn:
    - name: ipsec-policy-manager
      namespace: ipsec-policy-operator
  interval: 1m
  timeout: 30m
  test:
    enable: false
  install:
    disableHooks: false
  upgrade:
    disableHooks: false
  valuesFrom:
    - kind: Secret
      name: ipsec-policy-agent-static-overrides
      valuesKey: ipsec-policy-agent-static-overrides.yaml
    - kind: Secret
      name: ipsec-policy-agent-system-overrides
      valuesKey: ipsec-policy-agent-system-overrides.yaml